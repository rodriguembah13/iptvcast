{% extends 'base.html.twig' %}
{% block title %}
    Edit Agence
{% endblock %}
{% block body %}
    <div class="container-fluid">
        <form method="post">
            <div class="card">
                <div class="card-header">
                    <h4>Modify Agence: {{ agence.name }}</h4>
                </div>
                <div class="card-body">
                    <input id="id_chanel" value="{{ agence.id }}" type="hidden">
                    <div class="row mb-3">
                        <label for="exampleInputUsername2" class="col-sm-3 col-form-label">Name</label>
                        <div class="col-sm-9">
                            <input  type="text" name="name" value="{{ agence.name }}" class="form-control" id="name_chanel" placeholder="Name">
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="exampleInputEmail2" class="col-sm-3 col-form-label">Phone</label>
                        <div class="col-sm-9">
                            <input type="text" value="{{ agence.phone }}" class="form-control" name="phone" id="price_chanel" autocomplete="off"
                                   placeholder="Price">
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="exampleInputEmail2" class="col-sm-3 col-form-label">Adress</label>
                        <div class="col-sm-9">
                            <input type="text" value="{{ agence.address }}" class="form-control" name="address" id="price_chanel" autocomplete="off"
                                   placeholder="Adress">
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="exampleInputEmail2" class="col-sm-3 col-form-label">City</label>
                        <div class="col-sm-9">
                            <input type="text" value="{{ agence.city }}" class="form-control" name="city" id="price_chanel" autocomplete="off"
                                   placeholder="city">
                        </div>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary me-2" id="submit_chanel">Submit</button>
                    <button class="btn btn-secondary">Cancel</button>
                </div>
            </div>
        </form>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}


